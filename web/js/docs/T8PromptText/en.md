# T8 Prompt Text

A bundled multiline `STRING` source with no dependency on Comfyroll, EasyUse, or another custom-node package.

- Enter a prompt, API key, or any other string.
- The `text` output preserves the content unchanged and connects to compatible `STRING` inputs.
- The node does not call an API, inspect media, or rewrite its input.
