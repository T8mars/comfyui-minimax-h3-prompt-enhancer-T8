# T8 Directed Revision

Applies only the requested delta while preserving locked and unmentioned content. Uses one LLM request, then creates the diff locally. Non-empty unstructured output is preserved.
