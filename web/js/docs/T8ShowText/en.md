# T8 Show Text

A bundled `STRING` result viewer that removes the example workflows' dependency on third-party Show Text nodes.

- Connect an enhanced prompt, lyrics, Music Caption, or inspection report to `Text to display`.
- After execution, the complete value appears in a read-only preview with a copy button.
- The `text` output passes the input through unchanged for downstream use.
