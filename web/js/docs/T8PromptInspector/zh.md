# T8 提示词检查器

纯本地、确定性、非拦截的结构检查节点，支持 MiniMax H3、Seedance 2.0 和 MiniMax Music 3。

- `original_prompt` 始终逐字返回输入，不修改提示词。
- `warnings_json` 提示缺失核心字段、镜头编号、时间预算、素材角色、任务引用、稳定性、转场、说话人、字幕约束、Music 3 标题或语言冲突。
- `summary` 的分数只反映可机械检查的结构，不代表创意质量，也不调用 LLM。
- 所有提示都是建议，不会阻止下游继续执行。
